<section id="testimonials">
  <div class="container">
    <span class="section-label reveal">Testimonios</span>
    <h2 class="section-title reveal" style="transition-delay: 100ms">Lo que dicen alumnos y colegas</h2>
    <p class="section-subtitle reveal" style="transition-delay: 200ms">Feedback de estudiantes, clientes y colaboradores.</p>

    <div class="testimonials-slider reveal" style="transition-delay: 300ms">
      <div class="testimonials-track" id="testimonialsTrack">
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Christihan tiene una capacidad única para explicar conceptos complejos de IA de forma clara y práctica. Sus cursos son los mejores que he llevado en la facultad."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">AR</div>
            <div>
              <strong>Andrea R.</strong>
              <span>Alumna · Ing. Informática PUCP</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Excelente desarrollador Odoo. Implementó módulos personalizados para nuestra empresa con una calidad y velocidad impresionantes. Muy recomendado."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">CM</div>
            <div>
              <strong>Carlos M.</strong>
              <span>CEO · TechSolutions Perú</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Trabajar con Christihan en el proyecto de automatización fue una experiencia increíble. Su enfoque metódico y conocimiento técnico hicieron que todo fluyera."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">LG</div>
            <div>
              <strong>Lucía G.</strong>
              <span>COO · Distribuidora Norte S.A.</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Como colega en Vauxoo, admiro su dedicación y calidad de código. Sus contribuciones a Odoo Core son un reflejo de su excelencia técnica."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">JR</div>
            <div>
              <strong>Jorge R.</strong>
              <span>Tech Lead · Vauxoo</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"El acompañamiento en mi tesis fue excepcional. Me guió en todo el proceso de investigación con machine learning aplicado a robótica."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">MP</div>
            <div>
              <strong>Miguel P.</strong>
              <span>Tesista · Maestría PUCP</span>
            </div>
          </div>
        </div>
      </div>
      <div class="testimonials-nav">
        <button class="testimonial-btn" id="testPrev">←</button>
        <div class="testimonial-dots" id="testDots"></div>
        <button class="testimonial-btn" id="testNext">→</button>
      </div>
    </div>
  </div>
</section>

<style>
  #testimonials {
    background: var(--bg-secondary);
  }

  .testimonials-slider {
    overflow: hidden;
    position: relative;
  }

  .testimonials-track {
    display: flex;
    gap: 24px;
    transition: transform 0.4s ease-out;
  }

  .testimonial-card {
    min-width: calc(33.333% - 16px);
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .testimonial-stars {
    color: #f59e0b;
    font-size: 1rem;
    letter-spacing: 2px;
  }

  .testimonial-text {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.6;
    flex: 1;
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 12px;
    padding-top: 12px;
    border-top: 1px solid var(--border);
  }

  .testimonial-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--accent);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.8rem;
  }

  .testimonial-author strong {
    display: block;
    font-size: 0.9rem;
  }

  .testimonial-author span {
    font-size: 0.8rem;
    color: var(--text-secondary);
  }

  .testimonials-nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    margin-top: 24px;
  }

  .testimonial-btn {
    background: var(--bg-card);
    border: 1px solid var(--border);
    color: var(--text);
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 1rem;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .testimonial-btn:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .testimonial-dots {
    display: flex;
    gap: 8px;
  }

  .testimonial-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--border);
    cursor: pointer;
    transition: all 0.2s;
    border: none;
    padding: 0;
  }

  .testimonial-dot.active {
    background: var(--accent);
    width: 24px;
    border-radius: 4px;
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 1024px) {
    .testimonial-card {
      min-width: calc(50% - 12px);
    }
  }

  @media (max-width: 640px) {
    .testimonial-card {
      min-width: calc(100% - 0px);
    }
  }
</style>

<script>
  const track = document.getElementById('testimonialsTrack');
  const dotsContainer = document.getElementById('testDots');
  const cards = track.querySelectorAll('.testimonial-card');
  let currentSlide = 0;

  cards.forEach((_, i) => {
    const dot = document.createElement('button');
    dot.className = `testimonial-dot${i === 0 ? ' active' : ''}`;
    dot.addEventListener('click', () => goToSlide(i));
    dotsContainer.appendChild(dot);
  });

  function getVisible() {
    if (window.innerWidth <= 640) return 1;
    if (window.innerWidth <= 1024) return 2;
    return 3;
  }

  function goToSlide(index) {
    const visible = getVisible();
    const max = Math.max(0, cards.length - visible);
    currentSlide = Math.min(index, max);
    const card = cards[0];
    const gap = 24;
    const cardWidth = card.offsetWidth;
    track.style.transform = `translateX(-${currentSlide * (cardWidth + gap)}px)`;
    dotsContainer.querySelectorAll('.testimonial-dot').forEach((d, i) => {
      d.classList.toggle('active', i === currentSlide);
    });
  }

  document.getElementById('testPrev').addEventListener('click', () => {
    const visible = getVisible();
    goToSlide(Math.max(0, currentSlide - 1));
  });

  document.getElementById('testNext').addEventListener('click', () => {
    const visible = getVisible();
    goToSlide(currentSlide + 1);
  });

  window.addEventListener('resize', () => goToSlide(currentSlide));

  // Scroll animation
  document.querySelectorAll('.reveal').forEach(el => {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const delay = parseInt(el.style.transitionDelay) || 0;
            setTimeout(() => el.classList.add('visible'), delay);
            observer.unobserve(el);
          }
        });
      },
      { threshold: 0.15 }
    );
    observer.observe(el);
  });
</script>
