<section id="about">
  <div class="container">
    <span class="section-label reveal">Sobre mí</span>
    <h2 class="section-title reveal" style="transition-delay: 100ms">Academia · Industria · Innovación</h2>
    <p class="section-subtitle reveal" style="transition-delay: 200ms">
      Ingeniero Mecatrónico y Magíster en Computación por la PUCP. 
      +10 años de experiencia docente en inteligencia artificial y ciencia de datos.
      Python Developer en Vauxoo (Odoo Gold Partner). Fundador de Nexo Automatizaciones.
      Investigador con publicaciones en IEEE y Frontiers.
    </p>

    <div class="stats-banner reveal" style="transition-delay: 300ms">
      <div class="hero-stat">
        <span class="hero-stat-num"><span class="count-up" data-target="10">0</span>+</span>
        <span class="hero-stat-label">Años enseñando IA</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-num"><span class="count-up" data-target="822">0</span></span>
        <span class="hero-stat-label">Contribuciones Odoo</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-num"><span class="count-up" data-target="647">0</span></span>
        <span class="hero-stat-label">PRs fusionadas</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-num"><span class="count-up" data-target="3">0</span>+</span>
        <span class="hero-stat-label">Publicaciones IEEE</span>
      </div>
    </div>

    <div class="about-grid">
      <div class="about-card reveal" style="transition-delay: 100ms">
        <h3>🎓 Formación</h3>
        <ul>
          <li><strong>MSc Computación</strong> — PUCP (2025)</li>
          <li><strong>Ing. Mecatrónico</strong> — PUCP (2022)</li>
          <li><strong>MicroMasters</strong> — MITx / edX (2021)</li>
        </ul>
      </div>
      <div class="about-card reveal" style="transition-delay: 200ms">
        <h3>💼 Experiencia</h3>
        <ul>
          <li><strong>Docente</strong> — PUCP (2015–presente)</li>
          <li><strong>Python/Odoo Dev</strong> — Vauxoo (2023–presente)</li>
          <li><strong>Head of Engineering</strong> — qAIRa (2020)</li>
          <li><strong>Research Assistant</strong> — PUCP (2016–2018)</li>
        </ul>
      </div>
      <div class="about-card reveal" style="transition-delay: 300ms">
        <h3>🔧 Stack Tecnológico</h3>
        <div class="skills-cloud">
          <span>Python</span><span>Odoo</span><span>PostgreSQL</span>
          <span>TensorFlow</span><span>Keras</span><span>Scikit-learn</span>
          <span>Docker</span><span>Git</span><span>JavaScript</span>
          <span>OWL</span><span>OpenCV</span><span>Power BI</span>
          <span>R</span><span>C++</span><span>MATLAB</span>
        </div>
      </div>
      <div class="about-card reveal" style="transition-delay: 400ms">
        <h3>📚 Cursos que enseño</h3>
        <ul>
          <li>Inteligencia Artificial & Aprendizaje Automático</li>
          <li>Deep Learning</li>
          <li>Minería de Datos & Data Science</li>
          <li>Sistemas ERP (Odoo)</li>
          <li>Automatización Industrial 4.0</li>
          <li>Robótica & Visión Computacional</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .stats-banner {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 48px;
  }

  .hero-stat {
    text-align: center;
    padding: 24px 16px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    transition: transform 0.2s;
  }

  .hero-stat:hover {
    transform: translateY(-2px);
  }

  .hero-stat-num {
    display: block;
    font-size: 2rem;
    font-weight: 800;
    color: var(--accent);
    font-family: var(--font-mono);
    margin-bottom: 4px;
  }

  .hero-stat-label {
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }

  .about-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    transition: transform 0.2s, border-color 0.2s;
  }

  .about-card:hover {
    transform: translateY(-2px);
    border-color: var(--accent);
  }

  .about-card h3 {
    font-size: 1.1rem;
    margin-bottom: 16px;
    font-weight: 700;
  }

  .about-card ul {
    list-style: none;
    padding: 0;
  }

  .about-card li {
    padding: 6px 0;
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .about-card li strong {
    color: var(--text);
  }

  .skills-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .skills-cloud span {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 4px 12px;
    font-size: 0.85rem;
    color: var(--text-secondary);
    font-family: var(--font-mono);
    transition: border-color 0.2s, color 0.2s;
  }

  .skills-cloud span:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 768px) {
    .stats-banner {
      grid-template-columns: repeat(2, 1fr);
    }

    .about-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  let countersStarted = false;

  function animateCounters() {
    if (countersStarted) return;
    countersStarted = true;
    document.querySelectorAll('.count-up').forEach(el => {
      const target = parseInt(el.dataset.target);
      const duration = 1500;
      const start = performance.now();

      function update(now) {
        const elapsed = now - start;
        const progress = Math.min(elapsed / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        el.textContent = Math.floor(eased * target);
        if (progress < 1) requestAnimationFrame(update);
      }
      requestAnimationFrame(update);
    });
  }

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCounters();
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.3 }
  );

  const revealEls = document.querySelectorAll('.reveal');
  revealEls.forEach(el => observer.observe(el));

  document.querySelectorAll('.reveal').forEach(el => {
    const animObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const delay = parseInt(el.style.transitionDelay) || 0;
            setTimeout(() => el.classList.add('visible'), delay);
            animObserver.unobserve(el);
          }
        });
      },
      { threshold: 0.15, rootMargin: '0px 0px -40px 0px' }
    );
    animObserver.observe(el);
  });
</script>
